name: 3rd

on:
  workflow_dispatch:
  # schedule:
  #   - cron: '0 */5 * * *'

permissions:
  contents: write

jobs:
  poll-bms:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install Python Dependencies
        run: pip install requests curl-cffi

      - name: Configure Git User
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global pull.rebase true

      - name: Install and Setup Cloudflare WARP
        run: |
          # Add Cloudflare GPG key and repo
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          
          # Install WARP
          sudo apt-get update
          sudo apt-get install cloudflare-warp -y
          
          # Configure WARP in proxy mode (--accept-tos bypasses the interactive prompt)
          warp-cli --accept-tos registration new
          warp-cli --accept-tos mode proxy
          warp-cli --accept-tos connect
          
          # Wait a few seconds for the connection to establish
          sleep 5
          warp-cli --accept-tos status
          
          # Test the proxy to ensure it's routing through Cloudflare
          curl -x socks5://127.0.0.1:40000 https://1.1.1.1/cdn-cgi/trace | grep warp || echo "WARP Test Failed, but continuing..."

      - name: Fetch and Run Polling Script
        env:
          BMS_EMAIL: '${{ secrets.BMS_EMAIL }}'
          BMS_PHONE: '${{ secrets.BMS_PHONE }}'
          NTFY_TOPIC: '${{ secrets.NTFY_TOPIC }}'
          PROXY_LIST: '${{ secrets.PROXY_LIST }}'
          # Add the new secrets to the environment
          WORKER_URL: '${{ secrets.WORKER_URL }}'
          WORKER_PASSPHRASE: '${{ secrets.WORKER_PASSPHRASE }}'
        run: |
          curl -s -f -H "X-Worker-Passphrase: $WORKER_PASSPHRASE" "$WORKER_URL/test_4.py" -o test_4.py
          
          python -u test_4.py
          
      - name: Secure Cleanup
        if: always() # Ensures the file is deleted even if the python script above crashes
        run: rm -f test_4_fr.py
